//loading gif styles styles
const styles = theme => ({
  load: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "90vh"
  }
});

export default styles;

//profile page stuff. add students to groups
