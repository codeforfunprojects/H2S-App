// StudentList styles
const styles = theme => ({
  evalGrid: {
    paddingRight: theme.spacing.unit * 2
  },
  evalButton: {
    color: "white",
    width: 100
  },
  checkInButton: {
    paddingRight: theme.spacing.unit
  }
});

export default styles;
