// StudentList styles
const styles = theme => ({
  evalGrid: {
    paddingRight: theme.spacing.unit * 2
  },
  scrollCard: {
    maxHeight: 240,
    overflow: "auto"
  },
  evalButton: {
    color: "white",
    width: 100
  },
  checkInButton: {
    paddingRight: theme.spacing.unit
  }
});

export default styles;
